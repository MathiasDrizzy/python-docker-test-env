# Python + Docker + VSCode + Git + GitHub (Test)

Proyecto de prueba para validar el entorno:

- Python (API con FastAPI)
- Docker + docker-compose
- VSCode
- Git + GitHub CLI
- PowerShell con Oh My Posh

## Correr con Docker

```bash
docker compose up --build
